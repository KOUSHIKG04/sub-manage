import Screen from "@/src/components/Screen";
import { Text, View } from "react-native";

export default function Subscription() {
  return (
    <Screen>
      <Text className="text-primary text-lg">Subscription</Text>
    </Screen>
  );
}
